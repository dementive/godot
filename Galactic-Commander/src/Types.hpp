#include <cstdint>

namespace GC {
// typedefs for game object ids so they aren't just ambiguous integers
typedef uint64_t CommanderID;
typedef uint64_t SolarSystemID;
typedef uint64_t ColonyID;
typedef uint64_t ShipID;
typedef uint64_t StructureID;
typedef uint64_t StellarBodyID;
} //namespace GC